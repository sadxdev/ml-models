House Price Prediction model using Linear Regression


What’s Happening Internally?

Model finds best-fit straight line:

```price = m * size + b```


It learns m (slope) and b (intercept)

Uses them to predict new prices